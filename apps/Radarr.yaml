---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: radarr
  namespace: default
spec:
  chart:
    spec:
      chart: radarr
      version: 16.0.1
      sourceRef:
        kind: HelmRepository
        name: k8s-at-home
        namespace: flux-system
  dependsOn:
  - name: yasr-volume
  - name: main-volume
  interval: 1h0m0s
  values:
    image:
      pullPolicy: Always
      tag: latest
    env:
    - name: TZ
      value: "America/New_York"
    persistence:
      config:
        enabled: true
        existingClaim: yasr-volume
        subPath: configs/radarr
      data:
        enabled: true
        existingClaim: yasr-volume
        subPath: configs/transmission
      movies:
        enabled: true
        existingClaim: main-volume
        subPath: public/Movies
    ingress:
      main:
        enabled: true
        hosts:
        - host: movies1.${INGRESS_INTERNAL_NAME}
          paths:
          - path: /
        tls:
        - hosts:
          - movies1.${INGRESS_INTERNAL_NAME}
          secretName: movies1-k8s-cert
        annotations:
          kubernetes.io/ingress.class: "nginx"
          cert-manager.io/cluster-issuer: "ca-issuer"
