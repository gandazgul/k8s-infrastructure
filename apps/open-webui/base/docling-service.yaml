apiVersion: v1
kind: Service
metadata:
  name: docling
  namespace: default
  labels:
    app.kubernetes.io/name: docling
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: docling
  ports:
    - name: http
      protocol: TCP
      port: 5001
      targetPort: http
