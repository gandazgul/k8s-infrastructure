---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: skooner
  namespace: default
spec:
  dependsOn:
    - name: sealed-secret
  interval: 10m0s
  path: ./apps/skooner/
  prune: true
  sourceRef:
    kind: GitRepository
    name: k8s-infrastructure
    namespace: kube-system
  postBuild:
    substituteFrom:
      - kind: Secret
        name: secrets
