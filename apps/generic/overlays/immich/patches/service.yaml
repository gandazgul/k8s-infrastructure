apiVersion: v1
kind: Service
metadata:
  name: immich
spec:
  ports:
    - name: server-http
      port: 3001
      protocol: TCP
      targetPort: server-http
    - name: http
      port: 3000
      protocol: TCP
      targetPort: web-http
