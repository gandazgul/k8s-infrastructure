---
# Source: seedbox/templates/jackett-ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: seedbox-jackett-ingress
  labels:
    app.kubernetes.io/name: seedbox
    app.kubernetes.io/instance: seedbox
    app.kubernetes.io/version: "1.0"
  annotations:
    cert-manager.io/cluster-issuer: ca-issuer
    kubernetes.io/ingress.class: nginx
spec:
  tls:
  - hosts:
    - "jackett.${INGRESS_INTERNAL_NAME}"
    secretName: jackett-k8s-cert
  rules:
  - host: "jackett.${INGRESS_INTERNAL_NAME}"
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: seedbox-jackett-service
            port:
              number: 9117
---
# Source: seedbox/templates/radarr-ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: seedbox-radarr-ingress
  labels:
    app.kubernetes.io/name: seedbox
    app.kubernetes.io/instance: seedbox
    app.kubernetes.io/version: "1.0"
  annotations:
    cert-manager.io/cluster-issuer: ca-issuer
    kubernetes.io/ingress.class: nginx
spec:
  tls:
  - hosts:
    - "movies.${INGRESS_INTERNAL_NAME}"
    secretName: radarr-k8s-cert
  rules:
  - host: "movies.${INGRESS_INTERNAL_NAME}"
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: seedbox-radarr-service
            port:
              number: 7878
---
# Source: seedbox/templates/sonarr-ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: seedbox-sonarr-ingress
  labels:
    app.kubernetes.io/name: seedbox
    app.kubernetes.io/instance: seedbox
    app.kubernetes.io/version: "1.0"
  annotations:
    cert-manager.io/cluster-issuer: ca-issuer
    kubernetes.io/ingress.class: nginx
spec:
  tls:
  - hosts:
    - "tv.${INGRESS_INTERNAL_NAME}"
    secretName: sonarr-k8s-cert
  rules:
  - host: "tv.${INGRESS_INTERNAL_NAME}"
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: seedbox-sonarr-service
            port:
              number: 8989
---
# Source: seedbox/templates/transmission-ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: seedbox-transmission-ingress
  labels:
    app.kubernetes.io/name: seedbox
    app.kubernetes.io/instance: seedbox
    app.kubernetes.io/version: "1.0"
  annotations:
    cert-manager.io/cluster-issuer: ca-issuer
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/app-root: /web/
spec:
  tls:
  - hosts:
    - "transmission.${INGRESS_INTERNAL_NAME}"
    secretName: transmission-k8s-cert
  rules:
  - host: "transmission.${INGRESS_INTERNAL_NAME}"
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: seedbox-transmission-service
            port:
              number: 9091
