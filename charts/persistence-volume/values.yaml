---
# Default values for persistence-volume.
# WARNING !!! removing the package causes the physical removal of the volume

volumeName: main-volume

storage: 512Gi

storageClass:

path: /media/main

volumeMode: Filesystem
