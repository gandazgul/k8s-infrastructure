apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mqtt-aedes.fullname" . }}-test-connection"
  labels:
    {{- include "mqtt-aedes.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mqtt-aedes.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
