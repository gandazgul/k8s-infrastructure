{{- if .Values.transmission.service.peerPort }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "helpers.peer-port-service" . }}
  labels:
    app: {{ include "helpers.name" . }}
    chart: {{ include "helpers.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: NodePort
  ports:
    - name: peer-port
      nodePort: {{ .Values.transmission.service.peerPort }}
      port: {{ .Values.transmission.service.peerPort }}
      protocol: TCP
      targetPort: peer-port
  selector:
    app: {{ include "helpers.name" . }}
    release: {{ .Release.Name }}
  {{- end }}