apiVersion: v1
kind: Service
metadata:
  name: {{ include "seedbox.fullname" . }}
  labels:
    app: {{ include "seedbox.name" . }}
    chart: {{ include "seedbox.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
{{- if contains "NodePort" .Values.service.type }}
      nodePort: {{ .Values.service.port }}
{{- end }}
      port: {{ .Values.service.port }}
      protocol: TCP
      targetPort: http
  selector:
    app: {{ include "seedbox.name" . }}
    release: {{ .Release.Name }}
