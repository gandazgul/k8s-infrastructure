# Default values

# These configs are taken mostly unmodified from:
# https://github.com/MaZderMind/hostpath-provisioner/tree/master/manifests
# Copyright (c) 2019 Peter Körner
# MIT Licence

replicaCount: 1

image:
  repository: quay.io/kubevirt/hostpath-provisioner
  tag: latest
  pullPolicy: IfNotPresent

filesystemPath: /var/kubernetes
pvReclaimPolicy: Retain

resources: {}
  # We usually recommend not to specify default resources and to leave this as a conscious
  # choice for the user. This also increases chances charts run on environments with little
  # resources, such as Minikube. If you do want to specify resources, uncomment the following
  # lines, adjust them as necessary, and remove the curly braces after 'resources:'.
  # limits:
  #  cpu: 100m
  #  memory: 128Mi
  # requests:
  #  cpu: 100m
#  memory: 128Mi

nodeSelector: {}

tolerations: []

affinity: {}
