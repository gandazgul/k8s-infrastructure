apiVersion: v1
kind: Service
metadata:
  name: {{ include "mqtt-aedes.fullname" . }}
  labels:
    {{- include "mqtt-aedes.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mqtt-aedes.selectorLabels" . | nindent 4 }}
