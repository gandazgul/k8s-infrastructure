persistence:
  config:
    enabled: true
    existingClaim: yasr-volume
    mountPath: /mnt/sync
    subPath: configs/resilio
  sync:
    enabled: true
    existingClaim: main-volume
    mountPath: /sync
    subPath: btsync
