---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: mqtt-broker
  namespace: default
spec:
  chart:
    spec:
      chart: mosca
      version: 0.2.0
      sourceRef:
        kind: HelmRepository
        name: gandazgul
        namespace: flux-system
  interval: 1h0m0s
  values:
    image:
      tag: "v2.8.3"
    service:
      type: NodePort
