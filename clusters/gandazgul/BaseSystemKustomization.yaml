apiVersion: kustomize.toolkit.fluxcd.io/v1beta1
kind: Kustomization
metadata:
  name: base-system
spec:
  interval: 10m0s
  path: ./clusters/gandagul/base-system/
  sourceRef:
    kind: GitRepository
    name: flux-system
  validation: client
  prune: true
  postBuild:
#    substitute:
#      cluster_env: "prod"
#      cluster_region: "eu-central-1"
    substituteFrom:
#    - kind: ConfigMap
#      name: cluster-vars
    - kind: Secret
      name: secrets

# use ${var:=default} or ${var} in the files to place variables
