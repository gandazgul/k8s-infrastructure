#####################################################################################################################
# COPY THIS FILE AS secrets.env TO YOUR CLUSTER FOLDER AND FILL IT OUT
#####################################################################################################################

# A password used for a couple of services
ADMIN_PASSWORD=rafa23
# An email address for notifications
EMAIL=rafaelgil83@gmail.com
# The smtp password to authenticate with gmail for sending notifications (application passwords on your google account)
SMTP_PASSWORD=dtvrmpkvnrzwnczf
# VPN User name
VPN_USER=p7407642
# VPN Password
VPN_PASSWORD=tvCmeU66Zq
# The city where the server is located or the result of loading this on the same network as the server: https://ifconfig.co/city
VPN_CITY=New Rochelle
# The token bitwarden uses to encrypt the admin settings. Random, obtained via: `openssl rand -base64 48`
BITWARDEN_ADMIN_TOKEN=we2fz+WDe6nm9ICsAlURV8EbRaxUbvy4J7DS0REsqeHwVqUsnH2bHLLJMMpTTNSA
# The token paperless uses to encrypt the username and passwords. Random, obtained via: `openssl rand -base64 48`
PAPERLESS_SECRET_KEY=gIOyyPL834WNoxZsLF9H8dTYrSjLKQMy+kAtMNfqCL7OjTqhwJ9AGs/48qxUHlHz
# The time zone to apply to all containers
CLUSTER_TIME_ZONE=America/New_York
# ACME DNS credentials.
# consider running our own: https://github.com/joohoi/acme-dns
# Obtained from "curl -s -X POST https://auth.acme-dns.io/register"
# tested with: curl -X POST -H "X-Api-User: $ACME_DNS_USERNAME" -H "X-Api-Key: $ACME_DNS_PASSWORD" \
               #-d '{"subdomain": "$ACME_DNS_SUBDOMAIN", "txt": "1234567890123456789012345678901234567890123"}' \
               #  https://auth.acme-dns.io/update
ACME_DNS_USERNAME=7012ff46-9032-4b07-916a-ba99d2c49262
ACME_DNS_PASSWORD=IfDdqOvVw_FgpsoXMDQaoDUvNuA7LgLrGcvwsR4G
ACME_DNS_SUBDOMAIN=c91f42a2-ee00-455f-8b18-2c7182aa3f76

# required - ingress config
# Services will use this name for their ingresses (get one free at https://noip.com)
INGRESS_INTERNAL_NAME=rafag.tk
# Gogs and any other services exposed to the outside world will use this name (get one free at https://www.freenom.com/ *.tk and others are free)
INGRESS_EXTERNAL_NAME=rafag.tk

# Master node information. These 2 values are filled in automatically.
MASTER_IP=10.1.1.2
MASTER_NODE_NAME=k8s-master
