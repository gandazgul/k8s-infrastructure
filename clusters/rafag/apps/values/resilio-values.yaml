persistence:
  config:
    enabled: true
    existingClaim: yasr-volume
    mountPath: /config
    subPath: configs/resilio/rafag/config
  sync:
    enabled: true
    existingClaim: main-volume
    mountPath: /sync
    subPath: rafag
