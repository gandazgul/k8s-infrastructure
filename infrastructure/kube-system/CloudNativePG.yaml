apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: cloudnative-pg
  namespace: kube-system
spec:
  chart:
    spec:
      chart: cloudnative-pg
      version: 0.22.1
      sourceRef:
        kind: HelmRepository
        name: cloudnative-pg
        namespace: kube-system
  dependsOn:
  - name: sealed-secrets-controller
  interval: 1h0m0s
  values:
    # Default values are usually fine for CloudNativePG
    # It will install the operator and CRDs
